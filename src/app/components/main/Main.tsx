import { Table } from '@/components';

export function Main() {
  return (
    <main className="h-screen flex-grow bg-zinc-200 px-4 py-4 flex flex-col gap-6">
      <header>despues vemos si ponemos un header</header>

      <Table />
    </main>
  );
}
